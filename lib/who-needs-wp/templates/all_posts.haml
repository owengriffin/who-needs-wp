%h1 All Posts
%ul
  - posts.each do |post|
    %li
      %span.date= post.created_at.strftime('%Y-%m-%d')
      %a.title{:href => "#{post.url}", :title => post.title}= post.title
      %p= post.summary
